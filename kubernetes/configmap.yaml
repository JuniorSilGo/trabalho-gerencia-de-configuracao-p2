apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: mapub

data:
  DB_HOST: "pgsql-junior-gc-p2.postgres.database.azure.com"
  DB_PORT: "5432"
  DB_DATABASE: "appdb"

  AUTH_SERVICE_URL: "http://backend:3001"

  PORT: "3001"